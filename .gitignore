**/bin/
**/obj/
**/.vs/
token.key
.env

# Локальные секреты (токен бота и пр.) — не коммитить
*.local

# CFG-004: локальные базы ключей после хостового `dotnet run` — реестр секретов
keys.db
*.db

# Артефакты аудита
.audit/
